import React from "react";

const GainerLoser = ({
  ticker,
  price,
  change_amount,
  change_percentage,
  volume,
  name,
}) => {
  return (
    <div>
      <title>{name}</title>
    </div>
  );
};

export default GainerLoser;
